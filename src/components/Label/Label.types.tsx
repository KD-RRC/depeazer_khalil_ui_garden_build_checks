export interface LabelProps {
    /** The text displayed by the label */
    text: string;
    /** Text color of the label */
    textColor?: string;
    /** Background color behind the label */
    backgroundColor?: string;
    /** Whether the label is disabled (visually muted) */
    disabled?: boolean;
  }